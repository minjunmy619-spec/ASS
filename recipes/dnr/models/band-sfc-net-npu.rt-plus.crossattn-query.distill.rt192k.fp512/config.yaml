_base_: ../band-sfc-net-npu.rt-plus.distill.rt192k.fp512/config.yaml

# Explicit alias for RT+ residual cross-attention-query SFC transport.
band_sfc_preset: rt_plus_crossattn_query
task:
  model:
    query_type: adaptive
