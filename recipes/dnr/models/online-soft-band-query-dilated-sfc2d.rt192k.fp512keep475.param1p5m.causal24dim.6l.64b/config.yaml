_base_: ../online-soft-band-query-dilated-sfc2d.rt192k.fp512keep475.causal24dim.6l.64b/config.yaml

# Adds a state-free frequency-pooled capacity branch after each dilated
# separator block. This raises trainable capacity to ~1.5M parameters while
# preserving the rt192k fp512 layer-cache shape.
online_pooled_mixer_hidden: 3328

task:
  model:
    pooled_mixer_hidden: ${online_pooled_mixer_hidden}
