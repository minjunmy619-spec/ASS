_base_: ../tf-mlpnet-edge.rt192k/config.yaml

# TF-MLPNet v3 "balance" preset (~6M parameters): hidden=208, 9 blocks, kf=7.
# Same NPU rules as edge (dilations=(1,2,4), state <= 192 KiB fp16).
tiger_variant: tf-mlpnet-balance
