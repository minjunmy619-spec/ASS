_base_: ../tf-mlpnet-edge.rt192k/config.yaml

# TF-MLPNet v3 "large" preset (~9M parameters): hidden=272, 8 blocks, kf=7,
# wider feature_dim=96. Still fits the 192 KiB fp16 DSP state quota because
# depth stayed at 8 and dilations are capped at 4.
tiger_variant: tf-mlpnet-large
